# TODO(bazel-ready): This assumes this repo is a submodule of sonic-buildimage.
common --registry=file://%workspace%/../../tools/bazel/registry
common --registry=https://bcr.bazel.build

build:stamp --stamp
build:stamp --workspace_status_command="./stamp_vars.sh"
# Stamp config disabled by default; it embeds git revision strings into
# binaries and requires the workspace to be a git repository.
# build --config=stamp
